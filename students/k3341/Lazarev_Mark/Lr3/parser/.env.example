# Образец: cp .env.example .env
# В Docker значение приходит из docker-compose (переменная окружения),
# этот файл нужен только для запуска парсера напрямую, без контейнера.
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5435/timemanager
